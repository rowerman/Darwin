scenario:
  id: AD-11
  name: "BadSuccessor dMSA Privilege Escalation"
  technique: "BadSuccessor (Design Flaw, no CVE)"
  difficulty: L3
network:
  dc_ip: 192.168.56.10
  domain: north.sevenkingdoms.local
attacker:
  credentials:
    username: lowpriv
    password: "Password123!"
    domain: north
target:
  windows_version: "Windows Server 2025 (DC)"
  patch_requirement: "MUST NOT have October 2025 patch installed"
  prerequisite: "CreateChild permission on any OU (present in ~91% of AD environments)"
  exploit: "Create malicious Delegated Managed Service Account (dMSA) → link to Domain Admin → Kerberos PAC inherits all SIDs → full Domain Admin"
flag:
  location: "Domain Admin NTLM hash + RC4 keys of target"
